import React, { createContext } from 'react'

const TableContext = () => {
      export const TableContextProvider = createContext(null)
      return (
            <     >
                  
            </>
      )
}

export default TableContext
